'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
export default function AdminLoginPage() { const router = useRouter(); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); async function submit(e: React.FormEvent) { e.preventDefault(); setLoading(true); setError(''); const res = await fetch('/api/admin/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ password }) }); const data = await res.json(); if (!res.ok) { setError(data.error || 'Não foi possível entrar.'); setLoading(false); return; } router.replace('/admin'); router.refresh(); } return <main className="container flex min-h-screen max-w-md items-center py-10"><form onSubmit={submit} className="card w-full p-8"><a href="/" className="text-sm text-gray-500">← Loja</a><p className="mt-10 text-sm font-bold uppercase tracking-[.18em] text-lime-700">EVOTrainer</p><h1 className="mt-3 text-3xl font-black tracking-tight">Área administrativa</h1><p className="mt-3 text-sm leading-6 text-gray-600">Digite a senha mestra para acessar as anamneses e aprovações.</p><label className="mt-7 block text-sm font-semibold">Senha<input autoFocus required type="password" value={password} onChange={e => setPassword(e.target.value)} className="field mt-2" /></label>{error && <p className="mt-4 text-sm text-red-600">{error}</p>}<button disabled={loading} className="btn btn-dark mt-6 w-full disabled:opacity-50">{loading ? 'Validando...' : 'Entrar'}</button></form></main> }
