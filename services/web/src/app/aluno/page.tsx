'use client';

import { Button, Notice, PageIntro, Panel } from '@/components/ui';
import { apiFetch } from '@/lib/api';
import { getSessionUser } from '@/lib/auth';
import { useEffect, useState } from 'react';
import { useRouter } from 'next/navigation';

export default function AlunoPage() {
  const router = useRouter(); const user = getSessionUser();
  const [sheets, setSheets] = useState<any[]>([]); const [form, setForm] = useState({ fatigueLevel: 5, jointPainLevel: 0, loadDifficulty: 5, message: '' }); const [passwords, setPasswords] = useState({ currentPassword: '', newPassword: '', confirmation: '' }); const [message, setMessage] = useState(''); const [passwordMessage, setPasswordMessage] = useState('');
  useEffect(() => { if (!user || user.role !== 'STUDENT') { router.replace('/login?next=/aluno'); return; } void apiFetch<{ spreadsheets: any[] }>(`/spreadsheets/${user.id}`).then(data => setSheets(data.spreadsheets)).catch(() => undefined); }, [router, user]);
  async function sendFeedback() { if (!user) return; try { await apiFetch('/feedbacks', { method: 'POST', body: JSON.stringify({ ...form, userId: user.id }) }); setMessage('Feedback enviado. Sua equipe já pode acompanhar sua evolução.'); setForm({ fatigueLevel: 5, jointPainLevel: 0, loadDifficulty: 5, message: '' }); } catch (error) { setMessage(error instanceof Error ? error.message : 'Não foi possível enviar.'); } }
  async function changePassword() { setPasswordMessage(''); if (passwords.newPassword !== passwords.confirmation) { setPasswordMessage('A confirmação não coincide com a nova senha.'); return; } try { await apiFetch('/auth/change-password', { method: 'POST', body: JSON.stringify({ currentPassword: passwords.currentPassword, newPassword: passwords.newPassword }) }); setPasswordMessage('Senha alterada com sucesso.'); setPasswords({ currentPassword: '', newPassword: '', confirmation: '' }); } catch (error) { setPasswordMessage(error instanceof Error ? error.message : 'Não foi possível alterar a senha.'); } }
  return <main className="mx-auto max-w-7xl px-5 py-10 lg:px-8"><PageIntro eyebrow="Área do aluno" title={`Olá, ${user?.name ?? ''}`} description="Acompanhe seus treinos, materiais, evolução e segurança da sua conta." action={<Button variant="dark" onClick={() => router.push('/treino')}>Abrir meus treinos</Button>} /><div className="grid gap-5 md:grid-cols-3">{['Treino A', 'Treino B', 'Treino C'].map(title => <Panel key={title} className="p-6"><p className="text-xs font-black uppercase tracking-wider text-emerald-300">Semana de treino</p><h2 className="mt-2 font-display text-2xl font-bold text-white">{title}</h2><p className="mt-2 text-sm text-slate-400">Consulte sua planilha e registre a execução.</p><a href="/treino" className="mt-5 inline-block font-bold text-emerald-300">Ver treino →</a></Panel>)}</div><div className="mt-7 grid gap-7 lg:grid-cols-2"><Panel className="p-6"><h2 className="font-display text-2xl font-bold text-white">Minhas Planilhas</h2><div className="mt-5 space-y-3">{sheets.length === 0 ? <p className="text-sm text-slate-400">Suas planilhas oficiais aparecerão aqui.</p> : sheets.map(sheet => <div key={sheet.id} className="flex items-center justify-between rounded-2xl bg-slate-900/70 p-4"><span className="font-bold text-slate-200">{sheet.title}</span><a className="font-bold text-emerald-300" href={sheet.fileUrl || sheet.externalUrl} target="_blank" rel="noreferrer">Abrir / baixar</a></div>)}</div></Panel><Panel className="p-6"><h2 className="font-display text-2xl font-bold text-white">Feedback da semana</h2><div className="mt-5 grid gap-4 sm:grid-cols-3">{([['fatigueLevel', 'Cansaço'], ['jointPainLevel', 'Dores'], ['loadDifficulty', 'Dificuldade']] as const).map(([field, label]) => <label key={field} className="text-sm font-bold text-slate-300">{label}<input className="mt-2 w-full accent-emerald-400" type="range" min={field === 'jointPainLevel' ? 0 : 1} max="10" value={form[field]} onChange={e => setForm({ ...form, [field]: Number(e.target.value) })} /><span className="text-emerald-300">{form[field]}/10</span></label>)}</div><textarea className="field-control mt-5 min-h-24" placeholder="Conte como foi sua semana..." value={form.message} onChange={e => setForm({ ...form, message: e.target.value })} />{message ? <div className="mt-4"><Notice kind={message.startsWith('Feedback') ? 'success' : 'error'}>{message}</Notice></div> : null}<Button className="mt-4" onClick={sendFeedback}>Enviar feedback</Button></Panel></div><Panel className="mt-7 max-w-2xl p-6"><h2 className="font-display text-2xl font-bold text-white">Segurança da conta</h2><p className="mt-2 text-sm text-slate-400">Troque sua senha sempre que desejar.</p><div className="mt-5 grid gap-3"><input className="field-control" type="password" placeholder="Senha atual" value={passwords.currentPassword} onChange={e => setPasswords({ ...passwords, currentPassword: e.target.value })} /><input className="field-control" type="password" minLength={8} placeholder="Nova senha (mínimo 8 caracteres)" value={passwords.newPassword} onChange={e => setPasswords({ ...passwords, newPassword: e.target.value })} /><input className="field-control" type="password" minLength={8} placeholder="Confirme a nova senha" value={passwords.confirmation} onChange={e => setPasswords({ ...passwords, confirmation: e.target.value })} />{passwordMessage ? <Notice kind={passwordMessage.includes('sucesso') ? 'success' : 'error'}>{passwordMessage}</Notice> : null}<Button onClick={changePassword}>Alterar senha</Button></div></Panel></main>;
}
